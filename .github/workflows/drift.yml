name: shipmate · drift
on:
  schedule:
    - cron: "17 3 * * *"   # nightly, off-peak
  workflow_dispatch:
permissions:
  contents: read
jobs:
  shipmate:
    name: shipmate
    uses: ship-iac/shipmate/.github/workflows/drift.yml@9e126104de5c11495b08a9d4e324daa6ce293551 # v0.25.0
    permissions:
      contents: read
      id-token: write
      actions: read
    secrets:
      SHIPMATE_APP_PRIVATE_KEY: ${{ secrets.SHIPMATE_APP_PRIVATE_KEY }}
    with:
      state_suffix: .state
      runs_on: ubuntu-slim
      # Empty covers every cell. Split the sweep by adding more files, one tag query each.
      tags: ""
